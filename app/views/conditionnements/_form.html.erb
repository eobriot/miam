<%= form_for [@conditionnement.aliment, @conditionnement] do |f| %>
  <% if @conditionnement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conditionnement.errors.count, "error") %> prohibited this conditionnement from being saved:</h2>

      <ul>
      <% @conditionnement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :quantite %><br />
    <%= f.text_field :quantite %>
  </div>
    <div class="field">
    <%= f.label :seuil %><br />
    <%= f.text_field :seuil %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
